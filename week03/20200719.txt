语法树
语法树与优先级的关系
最高是Membe运算
a.b 成员访问
super
带扩号的New的优先级比较高，带括号的New比较高
7种类型？

对象和Key
referen类型
应用类型在运行时进行时来处理删除或者是赋值的相关操作

函数调用
new a()['b']
先是带圆括号的new的对象访问b属性
先要处理圆括号的优先级

Expressions？

* 乘方
字符串的比较算法比较容易出错

双等号的使用要注意
短路逻辑要注意 不能保证写在里面的子结构都会被执行 要注意

拆箱转换
装箱关系

-语句
语句完成控制流程
if(x = 1)
return 10;
返回值？

while
for

声明
出现在函数的头部
预处理
会找到所有的var 来执行
所有声明都是预处理机制

作用域
const作用域就是在他的花括号里；可以分小结出来

语句的完成状态来决定

执行粒度
宏任务
微任务
函数调用

事件循环
等待 获取代码 执行代码 （时间，事件，

微任务
函数调用
Execution Context


Realm


尝试找出 JavaScript 引擎里面 Realm 所有的对象，使用一个 JS 数据可视化的框架去做一个可视化


需要补一边重学前端
